var Grid;
(function (Grid) {
    var MapScope = (function () {
        function MapScope() {
        }
        return MapScope;
    })();
    Grid.MapScope = MapScope;

    var GridController = (function () {
        function GridController() {
        }
        return GridController;
    })();
    Grid.GridController = GridController;
})(Grid || (Grid = {}));
//# sourceMappingURL=GridContractsController.js.map
